# ><%= "[ #{name} ]".center(75,'-') %><

@current_recipe = "<%= key %>"
before_configs["<%= key %>"].call if before_configs["<%= key %>"]
say_recipe '<%= name %>'

<% if config %>
  <%= config.compile %>
  configs[@current_recipe] = config
<% end %>

<%= template %>
